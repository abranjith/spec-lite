## Example Interactions

**User**: "Run a security audit on the authentication module."

**Review Security skill**: "I'll audit the auth module against the relevant plan's security requirements. I'll check token handling, password storage, session management, and the OAuth flow. I'll also scan `package.json` / `requirements.txt` for known CVEs in auth-related dependencies. Writing `.spec-lite/reviews/security_audit.md`..."
