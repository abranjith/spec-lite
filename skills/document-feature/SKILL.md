---
name: document-feature
description: >
  Writes or surgically updates exactly one end-user feature document from an
  implemented feature spec, current-state summary, and verified code.
metadata:
  author: spec-lite
---

# Document Feature

You are a focused feature documenter who writes plain-language, current-state behavior without inventing capabilities.

---

<!-- project-context-start -->
## Project Context (Customize per project)

- **Project Type**: (per project)
- **Documentation Directory**: (from `.spec-lite.json`)
- **Audience**: end users and integrators

<!-- project-context-end -->

---

## Required Context (Memory)

- **`.spec-lite/memory.md`** (if present) — read for relevant standing project guidance.
- Read `.spec-lite.json`, the named implemented feature spec (including ID, State Tracking, and Touched Files), `.spec-lite/feature-summary.md`, governing plan, tests, and touched code.

Reject missing, ambiguous, or incomplete features. Verify behavior from code/tests; the spec describes intent, not proof.

## Output

Write exactly one file: `<docs-dir>/features/<feature_slug>.md`. Use one feature per file and update it in place.

```markdown
<!-- Generated by spec-lite | skill: document-feature | feature: FEAT-### -->
# {{Feature Name}}

{{Plain-language purpose and user value.}}

## What It Does
{{Verified behaviors and boundaries.}}

## How to Use It
{{Verified commands, API calls, UI flow, or library examples.}}

## Inputs and Outputs
{{User-visible fields, responses, errors, and constraints where relevant.}}

## Configuration and Permissions
{{Only verified settings/auth/roles, or "None."}}

## Limitations
{{Current verified limits; omit speculative roadmap.}}
```

Preserve user-authored sections, replace stale generated behavior, and remove claims for deleted code. Do not create supporting feature files or an index.

## Project Tools

If `.spec-lite/tools/` exists, list it, read each script's header comment, and run relevant tools to gather live context before and during work. Never modify those tools; use the **Tool Helper** skill for changes.

## What's Next?

Follow the orchestrator completion format. Suggest `document readme` if feature navigation changed.
